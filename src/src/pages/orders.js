import Layout from '../components/layout/layout';

const Orders = () => {
  return (
    <Layout translate={false} h1="Fake order page" title="Fake order page">
      <p>Should be some data</p>
    </Layout>
  );
};

export default Orders;
